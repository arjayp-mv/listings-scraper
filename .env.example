# =============================================================================
# Amazon Reviews Scraper - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
# =============================================================================

# Server Configuration
HOST=0.0.0.0
PORT=8080

# Apify Configuration
# Get your API key from: https://console.apify.com/account/integrations
APIFY_API_KEY=your_apify_api_key_here

# Database Configuration (XAMPP MySQL defaults)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=amazon_reviews_scraper

# Worker Configuration
WORKER_INTERVAL_SECONDS=30
APIFY_DELAY_SECONDS=10
